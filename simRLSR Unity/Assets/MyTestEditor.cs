
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;
using System.IO;
using System.Linq;
using UnityEngine.UI;
using UnityEditor;
using System.Text;
using System.ComponentModel.Design;
using System.Security.Cryptography;
/*
[CustomEditor(typeof(AvatarBehaviors))]
public class MyTestEditor : Editor
{
    AvatarBehaviors avatarBehavior;
    void OnEnable()
    {
        avatarBehavior = (AvatarBehaviors)target;
    }

    /*
    public override void OnInspectorGUI()
    {
        avatarBehavior.type = (CommandType)EditorGUILayout.EnumPopup("Action", avatarBehavior.type);
        switch (avatarBehavior.type)
        {
            case CommandType.ActivateLeft:
                {
                    avatarBehavior.objName = EditorGUILayout.TextField("Name", avatarBehavior.objName);
                    avatarBehavior.addon = (GameObject)EditorGUILayout.ObjectField("Addon", avatarBehavior.addon, typeof(GameObject), true);

                    break;
                }
            case CommandType.ActivateRight:
                {
                    avatarBehavior.objName = EditorGUILayout.TextField("Name", avatarBehavior.objName);
                    avatarBehavior.addon = (GameObject)EditorGUILayout.ObjectField("Addon", avatarBehavior.addon, typeof(GameObject), true);
                    break;
                }
            case CommandType.Move:
                {
                    avatarBehavior.position = (GameObject)EditorGUILayout.ObjectField("Position", avatarBehavior.position, typeof(GameObject), true);
                    break;
                }
        }
    }*//*
}
*/